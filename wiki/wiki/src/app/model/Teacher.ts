export interface Teacher {
    name: string;
    degree: string;
    image: string;
    card: TeacherCard;
}
export interface TeacherCard {
    phone: number;
    email: string;
    homePage: string;
}
